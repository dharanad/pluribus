# How to add velox a library

1. Add Velox as sub module or download it into current project
2. run the helper scripts inside the velox
3. Start configuring root CMakeLists

Where to find Velox CMake options
How to add Velox as dependecy